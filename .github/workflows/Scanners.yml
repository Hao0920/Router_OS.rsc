name: Build Scanners.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: Build Scanners.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Data
        run:  |
          chmod +x ./Packages/Scanners/Workflows/Upstream.sh && ./Packages/Scanners/Workflows/Upstream.sh
      - name: Data Processing
        run:  |
          chmod +x ./Packages/Scanners/Workflows/Process.sh && ./Packages/Scanners/Workflows/Process.sh
      - name: Build Scanners.rsc
        run:  |
          chmod +x ./Packages/Scanners/Workflows/Build.sh && ./Packages/Scanners/Workflows/Build.sh
      - name: Clean Cache
        run: rm -rf ./Cache
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
