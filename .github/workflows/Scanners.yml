name: Build Scanners.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: Build Scanners.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Data
        run:  |
          chmod +x ./Scanners/Workflows/Upstream.sh && ./Scanners/Workflows/Upstream.sh
      - name: Data Processing
        run:  |
          chmod +x ./Scanners/Workflows/Process.sh && ./Scanners/Workflows/Process.sh
      - name: Build Scanners.rsc
        run:  |
          chmod +x ./Scanners/Workflows/Build.sh && ./Scanners/Workflows/Build.sh
      - name: Clean Cache
        run: rm -rf ./Scanners/Cache
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
