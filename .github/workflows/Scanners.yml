name: Build Scanners.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dScannersatch:
jobs:
  build:
    name: Build Scanners.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Code
        run:  |
          chmod +x ./Scanners/Workflows/Upstream.sh && ./Scanners/Workflows/Upstream.sh
      - name: Sorting Code Files
        run:  |
          chmod +x ./Scanners/Workflows/Sorting.sh && ./Scanners/Workflows/Sorting.sh
      - name: Build Scanners.rsc
        run:  |
          chmod +x ./Scanners/Workflows/Build.sh && ./Scanners/Workflows/Build.sh
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
