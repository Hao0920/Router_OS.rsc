name: Build Adblock.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: Build Adblock.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Data
        run:  |
          chmod +x ./Packages/Adblock/Workflows/Upstream.sh && ./Packages/Adblock/Workflows/Upstream.sh
      - name: Data Processing
        run:  |
          chmod +x ./Packages/Adblock/Workflows/Process.sh && ./Packages/Adblock/Workflows/Process.sh
      - name: Build Adblock.rsc
        run:  |
          chmod +x ./Packages/Adblock/Workflows/Build.sh && ./Packages/Adblock/Workflows/Build.sh
      - name: Clean Cache
        run: rm -rf ./Cache
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
