name: Build Adblock.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: Build Adblock.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Data
        run:  |
          chmod +x ./Adblock/Workflows/Upstream.sh && ./Adblock/Workflows/Upstream.sh
      - name: Data Processing
        run:  |
          chmod +x ./Adblock/Workflows/Process.sh && ./Adblock/Workflows/Process.sh
      - name: Build Adblock.rsc
        run:  |
          chmod +x ./Adblock/Workflows/Build.sh && ./Adblock/Workflows/Build.sh
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
