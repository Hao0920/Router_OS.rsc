name: Build ISP.rsc
on:
  schedule:
    - cron: "* 18 * * *"
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    name: Build ISP.rsc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get Upstream Data
        run:  |
          chmod +x ./Packages/ISP/Workflows/Upstream.sh && ./Packages/ISP/Workflows/Upstream.sh
      - name: Data Processing
        run:  |
          chmod +x ./Packages/ISP/Workflows/Process.sh && ./Packages/ISP/Workflows/Process.sh
      - name: Build ISP.rsc
        run:  |
          chmod +x ./Packages/ISP/Workflows//Build.sh && ./Packages/ISP/Workflows//Build.sh
      - name: Clean Cache
        run: rm -rf ./Cache
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
